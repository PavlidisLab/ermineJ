
// To make the help jar:
maven help

// To make the Windows installer
maven nsis
// [ for the jre version ]
maven nsis-jre 


// to make the source distribtion
maven dist:build-src

// to make the jnlp setup. Note that we are still using the help.jnlp which was hand-coded. The 1.4 jnlp plugin had to be 
// modified to include the extension element.
maven jnlp:generate-keystore
maven jnlp

// deploy the builds to the web site (must be able to ssh to the site)

scp target/jnlp/*.jar microarray:/proj/projectpages/ermineJ/webstart

scp target/distributions/* microarray:/proj/projectpages/ermineJ/distributions

scp -r web/* microarray:/proj/projectpages/ermineJ
